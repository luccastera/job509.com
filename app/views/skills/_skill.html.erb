<span class="inline-flex items-center bg-gray-100 text-gray-800 px-3 py-1 rounded-full text-sm" id="<%= dom_id(skill) %>">
  <%= skill.description %>
  <%= button_to resume_skill_path(skill),
                method: :delete,
                class: "ml-2 text-gray-500 hover:text-red-600",
                data: { turbo_confirm: t("helpers.confirm") } do %>
    &times;
  <% end %>
</span>

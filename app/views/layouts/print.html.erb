<!DOCTYPE html>
<html lang="<%= I18n.locale %>">
  <head>
    <title><%= content_for(:title) || "JOB509" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Zalando+Sans:wght@400;500;600;700&display=swap" rel="stylesheet">

    <style>
      body {
        font-family: 'Zalando Sans', Arial, sans-serif;
        font-size: 12pt;
        line-height: 1.5;
        color: #333;
        margin: 0;
        padding: 20px;
      }
      h1 { font-size: 24pt; margin-bottom: 10px; }
      h2 { font-size: 18pt; margin-top: 20px; margin-bottom: 10px; border-bottom: 2px solid #1D4ED8; padding-bottom: 5px; }
      h3 { font-size: 14pt; margin-top: 15px; margin-bottom: 5px; }
      .header { text-align: center; margin-bottom: 30px; }
      .section { margin-bottom: 20px; }
      .entry { margin-bottom: 15px; }
      .entry-title { font-weight: bold; }
      .entry-subtitle { color: #666; }
      .entry-date { font-style: italic; color: #888; }
      .skills-list { display: flex; flex-wrap: wrap; gap: 10px; }
      .skill-tag { background: #f0f0f0; padding: 5px 10px; border-radius: 4px; }
      @media print {
        body { margin: 0; padding: 15mm; }
        .no-print { display: none; }
      }
    </style>
  </head>

  <body>
    <div class="no-print" style="margin-bottom: 20px;">
      <button onclick="window.print()" style="padding: 10px 20px; background: #1D4ED8; color: white; border: none; border-radius: 4px; cursor: pointer;">
        Print
      </button>
      <button onclick="window.close()" style="padding: 10px 20px; background: #666; color: white; border: none; border-radius: 4px; cursor: pointer; margin-left: 10px;">
        Close
      </button>
    </div>

    <%= yield %>
  </body>
</html>
